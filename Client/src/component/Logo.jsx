import { HStack, Text } from "@chakra-ui/react";

export default function Logo(){
    return(
       <HStack justifyContent={"center"}>
            <Text as={"b"}>LOGO</Text>
       </HStack> 
    )
}